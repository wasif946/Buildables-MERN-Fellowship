/** @type {import('tailwindcss').Config} */
module.exports = {
  // This content array is crucial. It tells Tailwind where to find the classes it needs to generate.
  content: [
    "./src/**/*.{js,jsx,ts,tsx}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
